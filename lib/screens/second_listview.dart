import 'package:flutter/material.dart';

class SecondListView extends StatelessWidget {
  const SecondListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('두 번째 페이지'),
    );
  }
}
